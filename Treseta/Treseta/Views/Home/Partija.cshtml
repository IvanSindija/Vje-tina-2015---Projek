<h1>@ViewData["imeSobe"]</h1>
<div>
    <h3 id="igrac2">igrac3 </h3>
    <div>
        <label id="igrac1">igrac2</label>
        <canvas id="myCanvas" width="1000" height="700" style="border:1px solid #000000;"></canvas>
        <label id="igrac3">igrac4</label>
    </div>
    <h3 id="igrac0">@ViewData["korisnickoIme"]</h3>
</div>
<div class="container">
    <input type="text" id="message" />
    <input type="button" id="sendmessage" value="Send" />
</div>

<div id="chatBox" style="overflow-y: scroll; height:150px; border:1px solid #000000;">
    <ul id="discussion"></ul>
</div>

<link href="~/Content/Partija.css" rel="stylesheet" />
@section scripts {
    <!--Script references. -->
    <!--The jQuery library is required and is referenced by default in _Layout.cshtml. -->
    <!--Reference the SignalR library. -->
    <script src="~/Scripts/jquery.signalR-2.2.0.min.js"></script>
    <!--Reference the autogenerated SignalR hub script. -->
    <script src="~/signalr/hubs"></script>
    <!--SignalR script to update the chat page and send messages.-->
    <script>
        $(function () {
            // Reference the auto-generated proxy for the hub.
            var chat = $.connection.multiplayerHub;
            var name = @Html.Raw(Json.Encode(ViewData["korisnickoIme"]));
            var imeSobe = @Html.Raw(Json.Encode(ViewData["imeSobe"]));
            var c=document.getElementById("myCanvas");
            var ctx=c.getContext("2d");

            //zavrsetak partije
            chat.client.krajIgre = function(timAbodovi , timBbodovi){
                alert("timA = "+timAbodovi + ",timB = "+timBbodovi);
                returnToGlavniIzbornik(name);
            }

            //poziva se u novom krugu igre
            chat.client.noviKrug = function(){
                ctx.clearRect(360,190,300,320);
            }

            //dobiveni podatci o drubim igracima
            chat.client.podatciOIgracima = function(name1 , poz1, name2, poz2, name3, poz3){
                if(name1 != null)
                    $('#igrac'+poz1).text(name1.imeKorisnika);
                if(name2 != null)
                    $('#igrac'+poz2).text(name2.imeKorisnika);
                if(name3 != null)
                    $('#igrac'+poz3).text(name3.imeKorisnika);
            }

            //korisnik napustio usrid partije
            chat.client.pizda = function (nameL) {
                alert(nameL+"je napustio partiju");
                returnToGlavniIzbornik(name);
            };

            //servaer javlja da je korisnik napustio sobu
            chat.client.onUserDisconnected = function (name,brojIgraca) {
                $('#discussion').append('<li><strong>' + htmlEncode(name)
                   + '</strong>: ' + htmlEncode(" je napustio sobu") + '</li>');
                var objDiv = document.getElementById("chatBox");
                objDiv.scrollTop = objDiv.scrollHeight;
                $('#igrac'+brojIgraca).text("slobodno mjesto");
            };

            //servaer javlja da se korisnik spojio u sobu
            chat.client.onNewUserConnected = function (name , brojIgraca) {
                $('#discussion').append('<li><strong>' + htmlEncode(name)
                   + '</strong>: ' + htmlEncode(" se pridruzio sobi") + '</li>');
                var objDiv = document.getElementById("chatBox");
                objDiv.scrollTop = objDiv.scrollHeight;
                $('#igrac'+brojIgraca).text(name);
            };

            // Create a function that the hub can call back to display messages.
            chat.client.addNewMessageToPage = function (name, message) {
                // Add the message to the page.
                $('#discussion').append('<li><strong>' + htmlEncode(name)
                    + '</strong>: ' + htmlEncode(message) + '</li>');
                var objDiv = document.getElementById("chatBox");
                objDiv.scrollTop = objDiv.scrollHeight;
            };

            //upozorenja da nisam na potezu
            chat.client.upozorenje = function (msg){
                alert(msg);
            };

            //iscrtavanje bacene karte i broj igraca koji baca
            chat.client.bacenaJe = function ( karta , intBacac){
                var img = new Image();
                img.src = karta.pathSlike;
                img.onload = function (){
                    if(intBacac==0){
                        ctx.drawImage(img,460,410 ,parseInt(karta.sirina),parseInt(karta.visina));
                        ctx.clearRect(parseInt(karta.xPoz),590,parseInt(karta.sirina),parseInt(karta.visina));
                    }
                    if(intBacac==3){
                        ctx.drawImage(img,360,300,parseInt(karta.sirina),parseInt(karta.visina));
                    }
                    if(intBacac==2){
                        ctx.drawImage(img,460,190,parseInt(karta.sirina),parseInt(karta.visina));
                    }
                    if(intBacac==1){
                        ctx.drawImage(img,560,300,parseInt(karta.sirina),parseInt(karta.visina));
                    }   
                }
            }

            //dobije 10 karata i iscrta ih na platno
            chat.client.zapocniIgru = function(ruka){
                var imgs = [];
                var imagesOK=0;
                for(i=0;i<10;i++){
                    var karta = ruka[i];
                    var img = new Image();
                    imgs.push(img);
                    img.src = karta.pathSlike;
                    img.onload = function(){
                        imagesOK++; 
                        if (imagesOK>= 10  ){
                            for(j=0;j<10;j++)
                                ctx.drawImage(imgs[j],parseInt(ruka[j].xPoz),590,parseInt(ruka[j].sirina),parseInt(ruka[j].visina));
                        };  
                    };
                };
            };
            // Start the connection.
            $.connection.hub.start().done(function () {


                //spojin se u sobu
                chat.server.joinRoom(name , imeSobe);

                $("#myCanvas").click(function(evt) {
                    var  mouseX = parseInt(evt.clientX - c.offsetLeft);
                    var mouseY = parseInt(evt.clientY-c.offsetTop + window.pageYOffset);
                    var message = 'Mouse position: ' + mouseX.toString() + ',' + mouseY.toString();
                    if(mouseY > 590 && mouseY<690){//sirina karte j 100 i pocinje na 590
                        chat.server.cardClick(mouseX,mouseY , imeSobe);
                    }
                });

                $('#sendmessage').click(function () {
                    // Call the Send method on the hub.
                    chat.server.sendMessage(name,  imeSobe , $('#message').val());
                    // Clear text box and reset focus for next comment.
                    $('#message').val('').focus();
                });
            });
        });


        function returnToGlavniIzbornik(name){
            $.ajax({
                url: "Home/GlavniIzbornik",
                type: "GET",
                data: { korisnik: name },
            }).done(function(data) {
                alert("nesto se desilo");
            });
        }
        // This optional function html-encodes messages for display in the page.
        function htmlEncode(value) {
            var encodedValue = $('<div />').text(value).html();
            return encodedValue;
        }
    </script>
}

